# Requirements for Unified Models API Server
# Install with: pip install -r unified_api_requirements.txt

# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Core ML libraries (for both models)
# Note: numpy < 2.0 required for TensorFlow/MediaPipe compatibility
# The smile model was saved with numpy 2.x - it needs to be re-saved with numpy 1.x
numpy>=1.24.0,<2.0.0
pandas>=2.1.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Gesture model dependencies
tensorflow>=2.10.0,<2.16.0
mediapipe>=0.10.0

# Smile model dependencies
opencv-python>=4.8.0

# Optional but recommended
python-dotenv>=1.0.0

